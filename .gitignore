*.csv
!scripts/calibration/reference.csv
*.xlsx
tests/local/
cors-test.html
team-dashboard-spec.md
curve-rv-command.md

# RV-2 케이본드 원문 샘플 — 발화자 실명·전화번호 포함. 절대 커밋 금지.
# tools/mask-kbond-sample.mjs 로 마스킹한 결과물(tests/fixtures/kbond-sample.masked.txt)만 커밋한다.
tests/fixtures/raw-kbond/

# 로컬 전용 — 절대 커밋 금지 (API 키·사내 CA 번들)
.env
.env.*
*.pem
.corp-ca.pem

# 에너지 나우캐스트 백테스트 로컬 캐시(원시 FRED, 재생성 가능)
scripts/.nowcast-cache.json

# on/off 산출물 자동 백업(convert-onoff.mjs 가 덮어쓰기 직전 남김 — 로컬 복구용)
data/_backup/
