# [핸드오프] ST-1 채권 확장 — 단기물 원장에 장기등급 채권 편입

## 0. 컨텍스트

- 대상 모듈: `st1-quote-log.html` + `js/st1-parser.js` / `js/st1-curve.js` / `js/st1-ui.js`
- 기존 ST-1 은 **단기물 전용**(CP·예담·전단채·ABSTB, 등급 A1~A3/B/C/D)이었다.
  여기에 일반 채권(회사채·은행채·공사채·여전채·지방채, 장기등급 AAA~D)을 **같은 원장**에 기록한다.
- 성격: ST-1 은 **기록 도구**다. 측정도 판단도 하지 않는다. 채권이 들어와도 이 성격은 그대로다 —
  잔차 bp 와 좌표만 내고, 싸다·비싸다는 말하지 않는다.
- 상태: **Phase 1~2 완료.** 로컬 `main` 에 4커밋, 푸시 안 됨.

| 커밋 | 내용 |
|---|---|
| `6c0ed5f` | 파서 — 장기등급·섹터 파싱, `grade_scale`·`sector` 필드 |
| `f015027` | 계보 — 채권 계보 키, 동적 `seriesOrder` |
| `17221ab` | UI — 입력·필터·패널 분리 |
| `9e89ae4` | 화면 입력 종류 별칭을 파서와 동일하게 정규화 |

테스트: ST-1 149건 전부 통과. 레포 전체 기준선 실패 4건(`G5 · KR 2026` / `OO-1 Gate` / `OO-4 Gate` / `tools/test-phase3.js`)은 ST-1 무관, 착수 전과 동일.

## 1. 스키마 변경

행에 **두 필드가 늘었다.** 기존 필드는 하나도 안 바뀌었다.

```js
{
  // …기존 필드 그대로…
  kind: 'CP' | '예담' | '전단채' | 'ABSTB' | '채권' | <적은 그대로> | null,
  sector: '회사채' | '은행채' | '공사채' | '여전채' | '지방채' | null,   // 채권일 때만
  grade_scale: 'st' | 'lt' | null,                                      // 등급 체계
}
```

- **등급 열은 쪼개지 않았다.** 단기·장기를 `grade` 한 열에 담고 어느 체계인지만 `grade_scale` 에 남긴다.
  열을 둘로 나누면 기존 원장 행이 전부 한쪽만 채운 반쪽 행이 되고, 필터·차트가 두 열을 매번 합쳐야 한다.
- **파일 마이그레이션은 하지 않았다.** Phase 1 이전 행에는 두 필드가 없다. 읽는 쪽이 `?? null` 로 접는 것이 규약이다.
  (`data/st1/quotes.json` 은 현재 `rows: []` 라 실제 구행은 없다. 그래도 규약은 지킬 것.)
- `dedupeKey` 는 **맨 뒤에** `sector` 를 붙였다. 필드 없는 행은 `undefined` → 빈 문자열로 접혀 키가 안 바뀐다.
  중간에 끼워 넣으면 자릿수가 밀려 기존 행이 전부 새 행으로 계상된다. 회귀 테스트로 못 박아 뒀다.
- `grade_scale` 은 **dedupeKey 에 넣지 않았다.** 등급에서 유도되는 값이라, 추정이 흔들리면 같은 호가가 새 행으로 들어온다.

### 1.1 등급 정규화 규칙

| 입력 | 결과 | 이유 |
|---|---|---|
| `AA`, `BBB`, `BB` | `AA0`, `BBB0`, `BB0` | 부호 없는 표기와 `0` 표기는 같은 등급. 안 접으면 dedupeKey 가 갈려 원장이 두 줄로 쪼개진다 |
| `AAA` | `AAA` | 애초에 부호가 없는 등급 |
| `A` (단독) | `grade=null` + flag `ambiguous_grade` | 한 글자 A 를 인정하면 영문 상호·약칭의 A 가 전부 등급이 된다. `A+`/`A0`/`A-` 만 받는다 |
| `a2-` | `A2-` | 대문자 고정(기존 규약) |

`ambiguous_grade` 는 **예담의 "등급 없음"과 구분하기 위한 플래그**다. 예담은 등급이 원래 안 붙으므로 플래그가 없다.

### 1.2 겹치는 등급 글자

`B+` · `B` · `B-` · `C` · `D` 는 **두 체계에 똑같은 글자로 존재한다.** 이것만 문맥으로 푼다.

| 같은 줄의 종류 | `grade_scale` |
|---|---|
| CP · 예담 · 전단채 · ABSTB | `st` |
| 채권 계열 또는 섹터 토큰 | `lt` |
| 없음 — **원문 파싱 경로** | `lt` + flag `grade_scale_guessed` |
| 없음 — **화면 입력 경로** | `st` (플래그 없음) |

마지막 두 줄이 **의도적으로 다르다.** 판정 함수는 `resolveGradeScale` 하나를 공유하되, 화면 경로(`gradeScaleOf`)만
추측 결과를 단기로 되돌린다. 원문 한 줄에서 종류를 못 읽은 것과, 화면의 종류 칸이 아직 비어 있는 것은 다른 상태다 —
후자는 사용자가 곧 채울 칸이고 ST-1 의 기본 모집단은 단기물이라, 추측을 남기는 것보다 기본값을 주고 고치게 하는 쪽이 맞다.

### 1.3 flags 어휘 (쌓이는 순서)

```
ambiguous_grade  단독 A 가 발행사 잔여물에 남았다
grade_scale_guessed  겹치는 글자인데 종류가 없어 체계를 추측했다
kind_inferred  장기 등급만 보고 종류를 '채권' 으로 지었다 (섹터는 짓지 않는다)
no_rate · no_maturity · no_issuer  (기존)
```

## 2. 이번 세션에서 걸린 함정 4건

전부 **"고쳐놓고 보니 당연한데, 안 걸렸으면 조용히 틀렸을"** 종류다. 같은 자리를 또 밟지 않도록 남긴다.

### ① `AA0` 이 정규식에서 통째로 빠진다

교대문에 `AAA|AA+|AA-|AA|A+|A0|A-|…` 만 두면 입력 `"AA0"` 은 **아무것도 매치하지 않는다.**

```
"AA0" → AA 가 매치 시도 → 룩어헤드 (?![A-Za-z0-9]) 가 '0' 에 막힘
      → 한 칸 밀려 A0 매치 시도 → 룩비하인드 (?<![A-Za-z0-9]) 가 'A' 에 막힘
      → 매치 없음. grade = null, 플래그도 없음 (조용한 유실)
```

처리: 교대문에 `AA0`·`BBB0`·`BB0`·`B0` 을 **접두 포함순**으로 끼워 넣었다.
`GRADE_RE` 의 순서는 길이순이 아니라 **접두 포함순**이라는 점이 핵심이다 — `BBB` 계열이 `BB` 계열보다 먼저 와야
`"BBB+"` 가 `"BB"` 로 잘리고 남은 `"B+"` 가 다시 등급으로 새지 않는다.

### ② 등급(④)이 종류(⑤)보다 먼저 추출된다

겹치는 글자의 체계를 가르려면 종류가 필요한데, 추출 순서상 등급 판정 시점에 종류가 아직 없다.
순서를 바꾸면 `cut` 연쇄가 통째로 달라져 기존 오탐 차단이 전부 재검증 대상이 된다.

처리: **peek.** `matchKind()` 를 자르지 않고 조회만 하고, 실제 절단은 ⑤ 에서 한다.
peek 과 ⑤ 의 결과는 항상 같다 — 등급 매치는 룩어라운드 때문에 종류 토큰 안쪽에 걸릴 수 없어서,
④ 의 절단이 종류 토큰을 건드리지 못한다.

### ③ 계보 키에 하이픈이 들어간다

`^(예담|CP-A1|CP-A2|OTHER|채권-[^-]+-[^-]+)$` 같은 정규식은 **버킷이 하이픈으로 끝나는 키를 거부한다.**
`채권-여전채-AA-`, `채권-은행채-A-` 가 전부 탈락한다. 저장된 뷰에서 그 계보를 숨긴 상태가 다음 로드에 조용히 풀린다.

처리: `isSeriesKey()` 가 **첫 하이픈에서만 쪼갠 뒤**(섹터명에는 하이픈이 없다) 섹터를 `SECTOR_ORDER`,
버킷을 `GRADE_ORDER_LT` 와 대조한다. 목록이 바뀌면 자동으로 따라간다. 회귀 테스트에 두 키를 명시해 뒀다.

### ④ 화면 입력 경로가 파서와 갈린다

종류 칸에 `'회사채'`·`'카드채'`·`'전단'` 을 **직접 치면** 그 문자열이 그대로 `kind` 로 저장됐다.
원문 한 줄로 친 같은 호가와 다른 행이 되고, 원장에 `'회사채'` 라는 있지도 않은 종류가 생겨 계보·필터가 그 행을 못 본다.

처리: 파서가 `normalizeKind(text)` 를 export 하고 `rowFromPreview` 가 그것으로 접는다.
`matchKind` 를 그대로 쓰므로 **어휘의 단일 근원**이 유지된다. 같은 원칙으로 `normalizeGrade`·`gradeScaleOf` 도
파서에서 나온다. **화면이 자기 사전을 갖는 순간 두 경로가 갈린다** — 이 모듈에서 가장 반복해서 밟은 함정이다.

## 3. 계보 규칙

```
채권-{섹터}-{등급버킷}     예) 채권-은행채-AA0 · 채권-여전채-AA- · 채권-미분류-투기
```

- **섹터와 등급 둘 다로 가른다.** 같은 AA0 이라도 은행채와 여전채는 다른 곡선이다. 섞으면 적합선이
  둘 사이 어딘가를 지나 양쪽 잔차가 전부 거짓이 된다. 예담을 CP 에서 떼어낸 것과 같은 논리의 연장이다.
- **등급 버킷(`bucketLT`)**

  | 입력 | 버킷 |
  |---|---|
  | `AAA` `AA+` `AA0` `AA-` `A+` `A0` `A-` | 그대로 (한 노치가 실제로 커브를 가른다) |
  | `BBB+` `BBB0` `BBB-` | `BBB` |
  | `BB` 이하 전부 | `투기` |

  BBB 를 접고 BB 이하를 몬 것은 **건수 문제**다. 손으로 쌓는 원장에서 그 아래를 노치별로 쪼개면
  어느 쪽도 최소 3점을 못 채워 전부 적합 불가가 된다. → Phase 3 에서 실측 후 재검토(§6).
- **미분류 ≠ OTHER.** 섹터를 모르는 채권은 `채권-미분류-<버킷>` 으로 모은다.
  OTHER 로 보내면 파싱 실패 잔여물과 한 곡선에 섞인다.
- **등급 없는 채권 · `grade_scale !== 'lt'` 인 채권 → OTHER.** 곡선을 만들 근거가 없다.
- **계보 목록은 고정할 수 없다.** 섹터×등급 = 최대 54조합. 전부 깔면 토글이 화면을 덮고 빈 패널이 줄줄이 생긴다.
  `seriesOrder(rows)` 가 고정 계보 3개 + **원장에 실제로 있는 채권 조합만** `SECTOR_ORDER × GRADE_ORDER_LT` 순으로 + `OTHER` 맨 끝.
  계보 토글은 `renderChart` 에서 매번 다시 짓는다 — init 때 한 번 짓고 말면 첫 채권을 기록해도 토글이 안 생긴다.
- `SERIES_ORDER` 상수는 `@deprecated` 로 남아 있다. **고정 계보의 골격일 뿐 전체 목록이 아니다.** 새 소비처는 `seriesOrder()` 를 쓸 것.
- **적합 모델·가중·최소 3점 규칙은 무수정.** 채권 계보도 단기물과 완전히 같은 `r = a + b·ln(τ)` 를 쓴다.

## 4. v1 범위 밖 (의도적으로 안 만든 것)

- **민평 대비 호가 필드 없음.** 채권 호가는 절대금리만 받는다. 스프레드 호가를 받으려면 민평 커브가 필요하고,
  그 순간 ST-1 이 외부 데이터에 묶인다(= 클라이언트 온리 원칙 파기). 상대가치는 RV-1/RV-2 소관이다.
- **발행사 약칭 사전 없음.** `normalizeIssuer` 는 법인격 꼬리(`(주)`·`㈜`·`주식회사`)와 공백만 정리한다.
  숫자를 지우지 않는다(`SK증권3` 은 정보다). 약칭 매핑은 사전이 틀리면 조용히 다른 발행사로 합쳐지므로,
  실측으로 표기 분기를 본 뒤에 만든다.
- CD·KOFR 대비 스프레드, 알림, 임계값 — 기존 ST-1 범위 밖 그대로.

## 5. 실측에서 볼 것 (Phase 3 착수 전 필수)

Phase 3 백로그는 **전부 실측 결과에 달려 있다.** 원장이 실제로 쌓이기 전에 손대지 말 것.
숫자 없이 고치면 있지도 않은 문제를 고치게 된다.

### ① 필드별 파싱 실패율

`flags` 를 필드별로 집계한다. 무엇을 못 읽는지가 종류 칸 select 전환·약칭 사전의 **유일한 근거**다.

```js
// 스케치 — data/st1/quotes.json 에 대고 돌린다
const tally = {};
for (const r of rows) for (const f of r.flags ?? []) tally[f] = (tally[f] ?? 0) + 1;
// 보는 것: no_issuer / ambiguous_grade / grade_scale_guessed / kind_inferred 의 건수와 비율
```

- `ambiguous_grade` 가 높다 → 단독 A 를 받아야 할 수도 있다(현재는 거부). 어떤 라인인지 `raw` 로 확인.
- `grade_scale_guessed` 가 높다 → 화면/파서 체계 판정 규칙(§1.2)을 재검토.
- `kind_inferred` 가 높다 → 종류 토큰 없이 치는 습관이 있다는 뜻. 종류 칸 select 전환의 근거.

### ② 3점 미만 계보 비율

`buildSeries(rows)` 결과에서 `points.length < 3`(= `MIN_FIT_POINTS`) 또는 `fit.ok === false` 인 계보의 비율.

```js
// 스케치
const s = buildSeries(rows, { refDate: todayLocal() });
const keys = Object.keys(s);
const thin = keys.filter((k) => s[k].points.length < 3 || !s[k].fit.ok);
// 보는 것: thin.length / keys.length, 그리고 thin 안에 채권 계보가 몇 개인지
```

- 이 비율이 높으면 **계보를 너무 잘게 쪼갠 것**이다. 섹터를 합치거나(여전채+회사채) 등급 노치를 더 접어야 한다.
- 반대로 `투기` 버킷에 점이 몰려 있으면 버킷을 쪼갤 때가 된 것이다.
- 즉 §6 의 1·2번은 **같은 숫자를 보고 반대 방향으로 판단**한다. 먼저 재보고 결정할 것.

## 6. Phase 3 백로그 — **전부 실측 후 착수**

1. **OTHER 계보 정리.** 지금 OTHER 에는 파싱 실패 잔여물, 등급 없는 CP, A3 CP, 전단채, ABSTB,
   등급 없는 채권이 **전부 한 곡선에** 떨어진다. 그 선에 대한 잔차는 아무 의미가 없다.
   최소한 "적합선을 긋지 않는 보관 버킷"으로 바꾸거나 계보를 더 나눌 것. (근거: §5 ②)
2. **`투기` 버킷 분리.** 현재 BB+ ~ D 가 한 곡선. 건수가 쌓이면 BB / B / CCC 이하로 나눈다. (근거: §5 ②)
3. **종류 칸 select 전환.** 지금은 free text 라 사용자가 `'회사채'` 를 쳐도 `normalizeKind` 가 접어준다(§2 ④).
   다만 아는 어휘가 아니면 적은 그대로 남으므로, 오타가 원장에 들어갈 수 있다. select 로 바꾸면 근본 해결이지만
   자유 입력의 속도를 잃는다 — `kind_inferred`·미지 어휘 건수를 보고 결정. (근거: §5 ①)
4. **발행사 약칭 사전.** 표기 분기(`한국전력` / `한전` / `KEPCO`)가 실제로 얼마나 생기는지 본 뒤에.
   사전이 틀리면 **다른 발행사가 조용히 합쳐진다** — 파싱 실패보다 나쁘다. (근거: §5 ①)

## 7. 하지 말 것

- **화면이 자기 정규화 사전을 갖지 말 것.** 종류·등급·만기·체계 판정은 전부 `js/st1-parser.js` 에서 가져다 쓴다
  (`normalizeKind` · `normalizeGrade` · `gradeScaleOf` · `parseMaturityField`). 이번 세션 함정 4건 중 2건이 이 위반이었다.
- **`dedupeKey` 필드를 중간에 끼워 넣지 말 것.** 추가는 항상 맨 뒤. 자릿수가 밀리면 기존 원장이 통째로 재계상된다.
- **`grade_scale` 을 행에 들고 다니며 재사용하지 말 것.** 등급에서 유도되는 값이라 저장 직전에 다시 계산한다.
  들고 다니면 등급만 고쳤을 때 체계가 옛 값으로 남는다.
- **`SERIES_ORDER` 를 계보 전체 목록으로 쓰지 말 것.** `seriesOrder(rows)` / `isSeriesKey()` 를 쓴다.
- **계보 키를 정규식으로 검증하지 말 것.** 버킷에 하이픈이 들어간다(§2 ③).
- **`PREVIEW_FIELDS` 에 입력 칸 없는 필드를 넣지 말 것.** DOM 바인딩이 이 배열을 돌며 `st1-f-<필드>` 를 찾는다.
- **적합 모델(`fitLog`)·가중·`MIN_FIT_POINTS` 를 채권 때문에 손보지 말 것.** 단기물과 같은 모델을 쓰는 것이 설계다.
- **판단어 금지 (기존 원칙).** 싸다·비싸다·저평가·타이트를 쓰지 않고, 색으로 매수·매도를 암시하지 않는다.
  점 밝기는 오직 경과 시간이며 그래서 단색 명도만 바꾼다.
- **실측 없이 Phase 3 에 착수하지 말 것.** §6 의 1·2번은 같은 숫자를 보고 반대 방향으로 판단한다.
