# RV-2 케이본드 호가 랭킹 스크리너 — Phase 0 감사 보고

작성일: 2026-08-04
상태: **승인 완료** (민수, 2026-08-04) — Phase 1 착수
대상 명령서: RV-2 케이본드 호가 랭킹 스크리너 (bpbybp/fi-dashboard)

---

## 1. 저장소 현황

`git fetch` 완료. `origin/main`과 **0/0 동기화** (커밋 `659821c`). 작업 트리에 추적 대상 변경 없음.

### 1.1 테스트 베이스라인 (`node --test`, 인자 없이 자동탐색)

| 실패 | 위치 | 성격 |
|---|---|---|
| `cp-q3c-hikes.test.mjs` | `_backup_untracked/` | 미추적 백업 디렉터리를 러너가 주워감 |
| `OO-1 Gate` | `tests/local/anchors.local.test.mjs` | 로컬 실데이터 앵커 드리프트 (61행 vs 기대 40행) |
| `OO-4 Gate` | `tests/local/anchors.local.test.mjs` | 로컬 실데이터 앵커 드리프트 (`liquidity` vs 기대 `period`) |
| `tools/test-phase3.js` | `tools/` | 테스트가 아닌 도구 파일을 러너가 주워감 |

명령서가 말한 **4건 pre-existing failure와 정확히 일치**. Phase 4에서는 이 4건 외 신규 실패만 확인한다.
3건은 테스트 로직 문제가 아니라 **러너 탐색 범위 문제**(`_backup_untracked/`, `tools/`)이며 별건 과제(§7).

---

## 2. Fenrir 이식성 — 이미 완료됨

명령서 §1.6이 예상한 "Fenrir trading-board 파서 참조 이식"은 **RV-1 Phase 2 커밋(`47a44ba`)에서 이미
수행되었고, 원본과 로직 diff가 0**임을 확인했다.

| bpbybp | Fenrir 원본 | 상태 |
|---|---|---|
| `js/rv-parser.js` (438줄) | `src/lib/parsers/kbond.js` (946줄) | 정규식·로직 동일. 차이는 주석/포맷 압축 + `matchIssuer` DI 주입 + `unparsed[]` 반환 추가 |
| `js/rv-abbrev.js` (37줄) | `issuer-abbreviations.js` (34줄) | **항목 완전 동일** (21항목). 주석만 추가 |
| `js/rv-matcher.js` (90줄) | `issuer-matcher.js` | 8단계 매칭 동일. Fenrir가 미배선한 abbreviation 단계까지 bpbybp는 배선함 |

→ **재이식 불필요.** RV-2는 이 3개 파일을 읽기 전용으로 import한다.

### 2.1 RV-2 요구 대비 실제 갭

| §1.3~1.5 요구 | 현황 | 조치 |
|---|---|---|
| 만기 (27.8.3 등) | ✅ `parseMaturity` 5패턴 | 재사용 |
| 등급 | ✅ `parseRating` | 재사용 |
| 민평 + 끝전 | ✅ `parseMinpyeong` | 재사용 |
| 딜러 전화번호 (중복제거 주 키) | ✅ `parseBroker` → `{broker, phone}` | 재사용 |
| 방향 팔자/사자/교체 | ✅ `parseSide` (교체→offer) | 재사용 |
| 오버N | ✅ `/오[버바]\s*(\d+\.?\d*)/` | 재사용 |
| 민평팔자 0bp | ✅ `spread_type='flat'` | 재사용 |
| 명시수익률 | ✅ `parseActualYield` | 재사용 |
| "+N원" 결측 | ✅ `spread_type='won'` 로 식별 가능 | `offset_bp=null` 매핑 |
| **언더N** | ❌ 없음 — 오버만 구현 | **rv2 계층에서 신규** (rv-parser 불변) |
| **체결마커 동/동통/대치** | ❌ 없음 (`거래완료`만 태그) | rv2 신규 |
| **수량 (100억, 50억\*5장)** | ❌ `volume: null` 하드코딩 | rv2 신규 |
| **offset_bp 산출** | ❌ §1.4 우선순위 레이어 자체가 부재 | rv2 신규 |

### 2.2 설계에 영향 주는 위험 3건

**(A) 사자 수요 패널(§3.3)이 현재 파서가 버리는 라인을 필요로 한다.**
`rv-parser.js:13`의 `NON_INDIVIDUAL_RE`가 `이내|이후|잔존|연내|N개월|N~N년`을 "일반관심"으로 제외한다.
그런데 §3.3이 집계하려는 "1.5년 특은 사자", "2~3년 매수관심"이 바로 이 패턴이다. RV-1에선 정확한
만기가 필수라 제외가 옳았지만, **RV-2에선 제외 대상이 곧 수요 신호**다.
→ **해결: rv2 프리패스가 버킷수요 레인으로 분기** (승인된 결정).

**(B) `입장하셨습니다`가 시스템 메시지 필터에 없다.**
`SYSTEM_MESSAGE_RE = /퇴장하였습니다|입장하였습니다/` — 명령서 §1.2가 명시한 존댓말형은 미매칭.
단순 누락이 아니라 **실질 오염**이다: `parseKbondLog`의 멀티라인 병합(`rv-parser.js:38`)이 이 라인을
직전 호가 메시지에 붙여버린다. (`parseIssuerRaw:205`의 제거 로직은 그 밴드에이드.)
→ **해결: rv2 프리패스가 `parseKbondLog` 호출 이전, 원문 라인 단위로 제거** (승인된 결정).

**(C) 중복 제거 의미론이 RV-1과 정반대다.**
RV-1 `accumulate()`는 동일 키를 **덮어쓴다**(`rv-screener-ui.js:213`). RV-2 §1.7은 레벨 변경 시
**append**(observations 배열 = v2 수명주기 데이터). 키도 다르다(RV-1: issuer+code+만기+side /
RV-2: 전화번호+종목+side+offset).
→ **저장 계층 재사용 불가, Phase 3에서 신규 작성.**

---

## 3. 페이지 배치 — 신규 페이지 (승인됨)

**결정: 신규 `rv2-quote-rank.html` + `js/rv2-*.js`.** 근거:

1. **데이터 소스가 완전히 다름.** RV-1은 인포맥스 4788 민평 xlsx 업로드가 전제(SheetJS 의존).
   RV-2는 민평이 호가 라인 안에 있어 업로드가 없다. 공유 상태 0.
2. **테마 충돌.** §3.5는 KB CI 라이트를 요구하는데 `rv-screener.html`은 다크 전용(`--bg:#0d1117`).
   라이트 선례는 `curve-phase.html`/`onoff-spread.html`의 `[data-cp-theme='light']` 토큰 세트.
3. **저장 스키마 충돌** (§2.2-C).
4. 확장 선례(README:40의 GC → curve-phase 내 섹션)는 데이터 도메인이 같았기 때문. 여기선 다르다.

### 3.1 파서 방식 — 프리패스 + 위임 래퍼 (승인됨)

`rv-parser.js`에는 **테스트가 하나도 없다**(`tests/`에 `rv-calc`, `rv-curves`만 존재). 직접 수정은
RV-1 회귀 위험이 크다. → `js/rv2-parser.js`가 래퍼가 되고, **`rv-parser` 호출 이전에 프리패스**를 둔다:

```
원문 텍스트
  └─ [rv2 프리패스] 라인 단위 사전 분류
       ├─ 시스템메시지(입장하셨습니다 포함) → 폐기·카운트만      ← 2.2-(B) 해결
       ├─ (나머지 → parseKbondLog 로 멀티라인 병합)
       │    └─ [메시지 단위 분류]
       │         ├─ 개별호가   → rv-parser 추출기 위임 + rv2 증강(offset_bp·수량·체결마커)
       │         ├─ 버킷수요   → rv2 자체 수요 파서                ← 2.2-(A) 해결
       │         └─ 미분류     → 원문 보존
```

`rv-parser.js`는 **불변**. 언더N의 RV-1 측 갭은 별건 과제로 분리(§7).

---

## 4. 불변 파일 리스트 (명시 선언)

RV-2 작업 중 **`git diff`가 비어 있어야 하는 파일**:

> **2026-08-05 부분 해제 (승인됨).** 백로그 (a) 커밋 — **B-6 수정에 한해서만**
> `js/rv-parser.js` · `js/rv-engine.js` 2개의 불변 선언을 해제한다(§7).
> **나머지 선언은 전부 유지**되며, (b) 커밋에서는 `rv-parser.js` 가 다시 불변으로 돌아간다.

- `js/rv-parser.js`, `js/rv-matcher.js`, `js/rv-abbrev.js` — 읽기 전용 import만
  (`rv-parser.js`는 (a) 커밋에서만 예외)
- `js/rv-engine.js`, `js/rv-cross.js`, `js/rv-screener-ui.js`, `rv-screener.html` — RV-1 전체
  (`rv-engine.js`는 (a) 커밋에서만 예외)
- `js/rv-calc.js`, `js/rv-backtest.js`, `js/rv-chart.js`, `js/rv-curves.js`, `js/rv-heatmap.js`,
  `js/rv-ui.js`, `curve-rv.html` — 별개 Curve RV 모듈
- 그 외 모든 `*.html`, `js/curve-phase/`, `js/gc/`, `data/`, `tools/`(신규 마스킹 스크립트 제외),
  기존 `tests/*.test.mjs`
- `DESIGN.md`, `README.md`

**변경 허용 파일** (신규 7 + 수정 2):

| 구분 | 파일 | Phase |
|---|---|---|
| 신규 | `tools/mask-kbond-sample.mjs` | 0 |
| 신규 | `js/rv2-parser.js` | 1 |
| 신규 | `tests/rv2-parser.test.mjs` | 1 |
| 신규 | `tests/fixtures/kbond-sample.masked.txt` | 1 |
| 신규 | `js/rv2-buckets.js` | 2 |
| 신규 | `tests/rv2-buckets.test.mjs` | 2 |
| 신규 | `rv2-quote-rank.html`, `js/rv2-ui.js` | 3 |
| 수정 | `js/nav.js` | 3 — **NAV_ITEMS에 1줄 추가. 이것이 유일한 기존 코드 파일 변경** |
| 수정 | `.gitignore` | 0 — 원문 샘플 보관 경로 제외 |

---

## 5. 샘플 라인 분류표 (§5-5)

대상: `tests/fixtures/kbond-sample.masked.txt` (2026-08-05 원문 1일치, 5,224줄).
산출: `js/rv2-parser.js` `parseRv2()` 무수정 실행. 아래 수치는 `tests/rv2-parser.test.mjs`
의 픽스처 회귀 테스트가 그대로 고정하므로, 파서가 바뀌면 테스트가 먼저 깨진다.

### 5.0 라인 유형 비율

원문 5,225줄 → 공백 25 · 시스템메시지 994 제외, 멀티라인 병합 460 → **메시지 3,842건**.

| 유형 | 건수 | 메시지 대비 |
|---|---:|---:|
| 개별 호가 | 3,596 | 93.6% |
| ├ 교체 (`replace` 태그) | 128 | 3.3% |
| ├ 체결마커 (동·동통 34 / 대치 4) | 38 | 1.0% |
| └ CP·CD·전단채 (분류하되 랭킹 제외) | 142 | 3.7% |
| 구간 수요 (사자 패널) | 129 | 3.4% |
| 미분류 | 117 | 3.0% |
| — 방향 없음 (채용공고·공지 등) | 27 | 0.7% |
| — 종목·구간 단서 없음 | 90 | 2.3% |

방향은 **offer 3,553 : bid 43**. 개별 호가는 사실상 전부 매도호가이고, 매수 수요는
개별 호가가 아니라 구간 수요 레인(129건)으로 표현된다 — §3.3 사자 패널을 별도 레인으로
분기한 Phase 0 결정(§2.2-A)이 실데이터로 확인됐다.

**시스템메시지 994건 중 존댓말형(`입장하셨습니다`)이 대부분이다.** rv-parser 의
`SYSTEM_MESSAGE_RE` 만 썼다면 이 994건이 멀티라인 병합으로 직전 호가에 붙었다 — §2.2-B는
가설이 아니라 이 샘플에서 실제로 발화하는 오염이었다.

### 5.0.1 오프셋 산출 결과

| basis | 건수 | 개별호가 대비 |
|---|---:|---:|
| `explicit` (명시수익률+민평) | 453 | 12.6% |
| `flat` (명시 민평팔자) | 243 | 6.8% |
| `under` (언더N) | 186 | 5.2% |
| `over` (오버N) | 38 | 1.1% |
| `bp` (−4bp 표기) | 12 | 0.3% |
| **산출 성공 계** | **932** | **25.9%** |
| 미상 계 | 2,664 | 74.1% |

**랭킹 가능(rankable, 오프셋 有·CP/CD 제외) = 932건 / 3,596건 = 25.9%.**

`under` 186건은 rv-parser 가 처리하지 못하는 표현이다(백로그 B-1). RV-2 자체 처리가 없었다면
이 186건은 `parseSpread` 폴백에 걸려 **flat 0bp 로 오인**됐을 것이다 — 실제 −3bp 안팎을
0으로 읽는 오류가 랭킹의 5.2%에 발생한다.

### 5.1 분류표 필수 행 — 실측

#### 필수행 1 — 레벨 없는 개별 호가

종목·만기는 있으나 수익률·언더/오버·민평팔자 표현이 모두 없는 라인.

| 항목 | 건수 |
|---|---:|
| **레벨 없는 개별 호가** | **2,150** (개별호가의 **59.8%**, 메시지의 56.0%) |
| ├ 만기 有 | 2,100 |
| ├ 만기 無 | 50 |
| ├ 민평 有 (← B-6 모집단) | 1,829 |
| └ 민평 無 | 321 |

`offset_basis='unknown'` 카운트(2,150)와 **모집단이 정확히 일치**함을 확인했다 — §5.1이
세운 등식이 실데이터에서 성립한다.

**이 절이 결정하는 것 두 가지:**

**(1) B-5 는 1순위에서 내려간다.** §7.1이 예측한 오염 메커니즘("만기 꼬리가 수익률로 채택")의
실측 빈도는 **3,842건 중 0건**이다. 방어 적용 전후로 `parseActualYield` 결과가 달라지는
메시지는 9건뿐이며, 9건 **전부 반대 방향**이었다(아래 필수행 1-a). 이유는 명확하다:
`parseActualYield` 의 `(\d+\.\d+)\s*(?:팔자|사자)` 는 **소수점 숫자가 방향어 바로 앞**에
있을 때만 발화하는데, 실데이터의 레벨 없는 호가는 만기를 라인 **맨 앞**에 두고 방향어는
맨 뒤에 둔다(`27.5.4(화) 지에스리테일34-2 (민 3.802) 팔자`). §7.1의 실증 예시
(`중금채 27.8.3 팔자`)는 만기와 방향어가 인접한 합성 라인이었다.
→ **B-5의 실제 노출도는 0.0%.** 1순위 승격 근거가 소멸했다. 아래 B-7·B-8로 순위를 넘긴다.

**(2) B-6 이 진짜 문제다.** 레벨 없는 호가 2,150건 중 **1,829건(85%)에 민평이 함께 있다.**
RV-1은 `parseSpread` 폴백이 이 라인들에 `flat 0` 을 붙이고 `rv-engine.js:59`가 이를
`y = 민평` 으로 채택하므로, **RV-1 횡단면의 절반 이상이 "민평에 판다"는 허구의 원괴리 0**
으로 채워진다. 버킷 중앙값(시장 베타 근사)을 0쪽으로 끌어당기는 규모가 §7의 예상보다 훨씬 크다.
RV-2는 `RV2_FLAT_RE` 로 명시 표현일 때만 flat 을 인정해 이미 방어돼 있다(explicit flat 243건만 인정).

#### 필수행 1-a — 방어의 역방향 오작동 (신규 발견)

| 항목 | 건수 |
|---|---:|
| 만기 방어가 **실제 레벨을 삭제**한 메시지 | 9 |

`3.30 팔자 (한양증권 …)` 처럼 만기 없이 수익률만 던지는 전단채/PF-ABCP 라인에서
`parseMaturity` 가 `3.30` 을 **만기 2026-03-30(confidence `low`)로 오인**한다. RV-2의 방어
(`matched_text` 를 가격 파서 입력에서 제거)가 그 위에서 돌면 **유일한 레벨이 지워진다.**
결과: 과거 만기(2026-08-05 이전) 호가 11건이 생기고 해당 라인은 `unknown` 으로 떨어진다.
→ 백로그 **B-9**. 규모는 작지만(0.2%) RV-2 자체 결함이므로 등재한다.

#### 필수행 2 — 오프셋 미상 사유별 분해

| basis | 건수 | 개별호가 대비 |
|---|---:|---:|
| `unknown` | 2,150 | 59.8% |
| `won_unresolved` (+N원만 있고 결과 수익률 없음) | 457 | 12.7% |
| `no_minpyeong` (수익률은 있으나 민평 앵커 없음) | 57 | 1.6% |
| **계** | **2,664** | **74.1%** |

§6.2가 요구한 `stats.offset_missing_by_basis` 노출은 **장식이 아니다.** 랭킹 테이블에서
빠지는 관측이 4건 중 3건이므로, 이 카운트를 숨기면 사용자는 화면의 932건을 전체로 오인한다.

#### 필수행 3 — 리프 버킷별 유효 관측 수 분포

**섹터·RWA 축은 산출하지 못했다.** 두 축의 귀속 상수가 `js/rv2-buckets.js`(Phase 2 산출물)에
있고 아직 존재하지 않는다. 지금 산출 가능한 **등급 × 만기(연 단위 6버킷)** 2축으로 대신하며,
Phase 2 보고에서 4축 정식 분포로 대체한다.

rankable 932건 → 리프 24개.

| | 리프 수 | 관측 수 |
|---|---:|---:|
| n ≥ 8 | 16 | 904 |
| n < 8 | 8 | 28 |

**2축에서는 n=8 가드가 거의 발화하지 않는다(관측의 3.0%만 롤업).** 그러나 이 결론을 그대로
Phase 2로 옮기면 안 된다 — 두 가지 이유로 4축에서는 정반대가 된다.

1. **등급 축이 사실상 비어 있다.** rankable 932건 중 **등급 미상 703건(75.4%)**.
   상위 3개 리프가 전부 `(등급미상)`이고 이 셋이 664건(71%)을 차지한다. 즉 지금 n≥8을
   만드는 것은 등급 축이 아니라 **등급 축의 붕괴**다.
2. 섹터·RWA 2축이 추가되면 리프 수가 배가된다. 등급이 붙은 229건을 섹터×RWA로 다시 쪼개면
   대부분의 리프가 한 자릿수가 된다.

→ **§6 결정 1("샘플 실측 후 재조정 여지")에 대한 답: n=8 상수는 유지하되, Phase 2에서
롤업이 기본 경로가 되는 것을 전제로 설계하라.** 그리고 **등급 미상 75%를 먼저 줄여야 한다** —
등급이 없으면 섹터×등급×RWA 리프 자체가 정의되지 않는다. 등급 미상의 상당수는 아래 B-7의
민평 표기 이형과 같은 라인에서 나온다(`민평 3.242%/ 끝.91/ 쿠폰 2.88%` 처럼 등급을 아예
쓰지 않는 데스크가 있다). 이건 파서 결함이 아니라 **원문에 정보가 없는 것**이므로,
Phase 2는 등급 미상을 별도 리프로 두고 롤업 대상으로 다뤄야 한다.

> **2026-08-05 결정 (승인됨 · §6 결정 7).** 등급은 **리프 축에서 내린다.**
> 발행사 → 대표등급 **역매핑으로 복구**한 뒤 **보조 축**으로 쓰고, 미상은 **`등급미상`
> 버킷으로 명시 분리**한다. 상세 설계는 Phase 2 착수 시 확정.

### 5.1.1 신규 발견 — 위임 파서 표기 이형 (백로그 B-7·B-8)

분류표를 만들며 rv-parser 의 추출기가 실데이터 표기를 놓치는 지점 두 곳을 실측했다.

| # | 갭 | 건수 | 영향 |
|---|---|---:|---|
| **B-7** | `parseMinpyeong` 이 `민평 3.242` / `민:3.957%` / `민평3.112` / `민,3.945` 를 놓친다. 인식하는 것은 `민 3.610`·`민3.517` 형태뿐 | **198** | 이 중 **46건은 명시수익률이 함께 있어**, 민평만 복구되면 `explicit` 로 승격한다 → rankable 932 → 978 (**+4.9%**) |
| **B-8** | `parseActualYield` 가 숫자와 방향어가 붙은 `3.602팔자` 를 놓친다 (`\s*` 는 있으나 `(\d+\.\d+)` 앞 경계에서 탈락) | **43** | `민+1원 3.602팔자` 류가 `won_unresolved` 로 떨어진다. 민평이 함께 있으므로 인식만 되면 전부 `explicit` |

표기 이형 빈도 내역 — `민평 N.NNN`×146, `민:N.NNN`×39, `민평N.NNN`×11, `민: N.NNN`×2.

**둘 다 rv-parser(불변) 소관이지만 RV-2 계층에서 폴백을 얹을 수 있다** — 언더N 을 rv2 자체
처리한 것과 같은 구조다. B-5가 실측으로 내려간 자리를 이 둘이 대신한다: 합쳐서
**rankable 을 25.9% → 최대 31%까지** 끌어올린다. 다만 Phase 1 승인 범위를 넘으므로 별건.

### 5.1.2 그 밖의 실측

| 항목 | 값 |
|---|---|
| 딜러 전화번호 식별 | 3,377 / 3,596 (93.9%). 브로커명만 108, 둘 다 없음 111 |
| — 점·공백 구분 번호(`6923.7519`) | `parseBroker` 정규식이 하이픈만 보므로 **미식별**. 중복제거 주 키가 브로커명으로 폴백한다 |
| 수량 파싱 | 296 / 3,596 (8.2%). `50억*5장` 형태 정상 |
| 구간 수요 만기 파싱 | 95 / 129. 실패 34건은 `7영업일 이내`·`국전전 사자관심` 처럼 연 단위 표현이 아예 없는 라인 |
| `parse_confidence` | high 911 / medium 20 / low 2,665 — low 가 곧 오프셋 미상 모집단이다 |

### 5.2 분류표 필수 행 (원 요구 — 근거 보존)

라인 유형 비율(호가/사자/교체/체결/노이즈/미분류)에 더해, 다음을 **별도 행으로 명시**한다:

| 필수 행 | 왜 |
|---|---|
| **레벨 없는 개별 호가** — 종목·만기는 있으나 수익률·언더/오버·민평팔자 표현이 모두 없는 라인 | **B-5 착수 시점의 판단 근거**(§7.1). 이 빈도가 곧 RV-1 랭킹 오염 노출도다. RV-2에서는 `offset_basis='unknown'` 카운트와 같은 모집단이다 |
| 오프셋 미상 사유별 분해 (`won_unresolved` / `unknown` / `no_minpyeong`) | §6.2 UI 카운트 노출의 실제 규모 확인 |
| 리프 버킷(섹터×등급×RWA)별 유효 관측 수 분포 | §6-1 표본 가드 n=8 유지/조정 판단 근거 — Phase 2 보고에 포함 |

### 5.3 마스킹 규약 (승인됨) + 실행 결과
- 발화자 실명 → 익명 치환 (`트레이더01` 형태, 결정론적·안정 매핑)
- 전화번호 → 더미 (`000-0001` 형태, 원본별 안정 매핑)
- 브로커 **회사·데스크명은 보존** — 딜러태그 파싱 검증에 필요하고 개인정보가 아님
- 원문은 `tests/fixtures/raw-kbond/` (gitignore)에 보관, **마스킹 결과물만 커밋**
- 매핑표는 원문과 같은 gitignore 경로에 출력, 커밋 금지

실행 결과: **발화자 882명 · 전화번호 231건 · 이메일 1건** 치환.

승인 규약 대비 **추가한 것 2건** (전화번호 규약의 누락분을 메운 것):

| 추가 | 이유 |
|---|---|
| 점·공백 구분 전화번호 | 하이픈 정규식만으로는 `010.8910.6328`·`02 6923 7535`·`6923.7519`·`6099 8544` 등 **8개 번호가 그대로 남았다**. 3파트(선두 0 강제)는 전역, 2파트는 **딜러태그 안에서만** 수집한다 — 2파트를 전역으로 돌리면 `한전1422 4000억` 같은 종목코드+수량 인접이 매칭돼 채권 데이터가 깨진다 |
| 이메일 | 채용공고 블록의 실명 기반 주소 1건(`masked01@example.com` 으로 치환) |

**구분자는 보존한다.** `parseBroker` 가 점·공백형을 못 잡는다는 사실 자체가 픽스처가
지켜야 할 실데이터 이형이기 때문이다(§5.1.2).

**잔여 실명 후보 확인 결과 — 사람 이름 0건.** 딜러태그 안 마스킹되지 않은 한글 토큰 76건을
전수 확인했고, 전부 회사·데스크명(교보·다올·케이프·상상인·코리아에셋·리딩·부국 채권금융 …)
이거나 매매 용어(교체·사자·팔자·민평·끝전·이표·쿠폰·수반·평일·후순위 …)였다.
직함(님·대리·팀장·상무 …) 앞 토큰 스캔, 태그 내 1자·5자+ 한글 토큰 스캔에서도 실명 없음.
1자 닉네임 `쉥`(`한양중년 쉥`) 1건은 실명이 아닌 데스크 핸들로 보아 **보존**했다 —
판단 근거를 남기니 뒤집을 값이면 알려달라.

---

## 6. 확정된 결정 항목

| # | 항목 | 확정값 | 비고 |
|---|---|---|---|
| 1 | 표본 가드 n | **8** (상수화) | 리프 버킷(섹터×등급×RWA)은 일상적으로 n<8이 되어 **롤업이 예외가 아닌 기본**이 될 소지가 있다. **샘플 실측 후 재조정 여지** |
| 2 | MAD 계수 k | **2.5** (상수화) | 소표본 강건성. 평균±표준편차 금지 |
| 3 | 페이지 배치 | **신규 `rv2-quote-rank.html`** | §3 |
| 4 | "수금채" 귀속 | **수협은행** | 이식된 `rv-abbrev.js`가 `"수금": "수협은행"`, `"수금(중앙회)": "수협중앙회"`로 확정하고 `"수출입": "한국수출입은행"`은 별도 항목. `rv2-buckets` 상수에 **수협은행의 섹터 귀속을 명시**한다 |
| 5 | 교체 호가 | **추가 작업 없음** | `parseSide`가 이미 교체→`offer`, `parseTags`가 `'replace'` 태그. 명령서 기본안과 일치 |
| 6 | 사자 만기 격자 | **연 단위 6버킷**: `~1y / 1–2 / 2–3 / 3–5 / 5–10 / 10y+` | 채팅 원문이 "1.5년", "2~3년"처럼 연 단위로 말한다. Z/S/C/L 매핑은 bpbybp 범위 아님(Fenrir 소관) |
| 7 | **등급 축의 위치** (2026-08-05 신규) | **리프 축에서 내린다.** 발행사 → 대표등급 **역매핑으로 복구**한 뒤 **보조 축**으로 쓴다. 등급 미상은 **`등급미상` 버킷으로 명시 분리** | §5.1 필수행 3 실측: rankable 932건 중 등급 미상 **75.4%**. 원문에 등급을 아예 쓰지 않는 데스크가 있어 파서로는 복구되지 않는다. 등급을 리프 축에 두면 리프의 71%가 `(등급미상)` 한 칸에 몰려 섹터×RWA 분해가 무의미해진다. **상세 설계는 Phase 2 착수 시 확정** |

### 6.1 명령서 §1.4 규칙 1의 부호 예시 불일치 — 정정 기록

명령서 §1.4 규칙 1은 공식과 예시가 서로 어긋난다.

- 공식: `offset_bp = (호가수익률 − 민평수익률) × 100`
- 예시: `"+0.5원 3.608 팔자 (민 3.610)" → +0.2bp`
- 실제: `(3.608 − 3.610) × 100 = −0.2bp`

**공식이 옳다.** `+0.5원`은 가격이 높다 = 수익률이 낮다 = **비싸게 내놓은 오퍼**이므로, §1.4가 굵게
명시한 부호 규약("+오프셋 = 민평 대비 높은 수익률 = 싸게 나온 오퍼")과 §3.2의 랭킹 의미론
(offset_bp 내림차순 = 싸게 나온 순), 그리고 규칙 2(언더N = −N)와 모두 **음수여야 일관된다**.

→ 구현은 **공식을 따르고**, 예시의 부호는 오기로 간주한다. 코드 주석·테스트·UI 범례에 규약을 명시.

### 6.2 Phase 3 UI 이월 요구사항 (Phase 1 승인 시 추가 확정)

**오프셋 미상 관측은 랭킹에서 빼되 숨기지 않는다.** `offset_basis`가 `unknown` / `won_unresolved` /
`no_minpyeong` 인 관측은 §1.4 규칙 4의 "+N원 결측"과 **동일 취급**한다:

- 랭킹 테이블 본문에서 제외 (정렬 불가)
- **파싱 결과 요약**에 사유별 카운트 노출 — `stats.offset_missing_by_basis` 를 그대로 쓴다
- **버킷 헤더**에 해당 버킷의 미상 건수 표시 (`n=12 · 오프셋 미상 3`)
- 근거: "측정만 한다 — 표본 부족을 숨기지 않는다"(§2.3)와 같은 원칙. 미상 건수가 크면
  그 버킷의 랭킹 자체가 대표성이 없다는 신호이므로 사용자가 알아야 한다

---

## 7. 백로그 (이번 범위 제외 — 별건 과제)

> ## ⚠️ 2026-08-05 우선순위 확정 (승인됨) — §5.1 실측 결과 반영
>
> | 순위 | 항목 | 근거 |
> |---|---|---|
> | **1** | **B-6** | 레벨 없는 호가의 85%가 민평 동반 → RV-1 횡단면의 **50.9% 오염** |
> | 2 | B-7 · B-8 | 표기 이형 241건. RV-2 계층 폴백으로 처리 가능, rankable +4.9%p |
> | 3 | B-9 | RV-2 자체 결함 9건 |
> | 하위 | **B-5** | 실측 0건으로 **강등**. **삭제하지 않는다** — 판단 근거 추적용으로 §7.1과 함께 보존 |
> | 미정 | B-1 · B-2 · B-3 · B-4 | |
>
> **B-6+B-7+B-8+B-9를 하나의 묶음으로 착수한다. Phase 2보다 먼저.**
> 전제는 그대로 — **특성화 테스트 위에서** 진행한다(현재 동작을 버그까지 포함해 고정한 뒤
> 의도적으로 깨뜨린다).
>
> ### 커밋 분리 (확정)
>
> | 커밋 | 범위 | 필수 산출물 |
> |---|---|---|
> | **(a)** | **RV-1 측 수정 — B-6.** `rv-parser.js` · `rv-engine.js` 변경 | **before/after 횡단면 diff 보고 필수** — 어떤 호가의 괴리·순위가 어떻게 바뀌는지 명시 |
> | **(b)** | **RV-2 계층 폴백 — B-7 · B-8** + **B-9 수정** | RV-2 범위 내. rv-parser 불변 |
>
> **불변 파일 리스트(§4) 해제는 (a)에 한한다.** `js/rv-parser.js`·`js/rv-engine.js` 2개만,
> (a) 커밋에서만 변경 허용. **나머지 불변 선언은 전부 유지**한다 — 특히 (b)에서는
> `rv-parser.js` 가 다시 불변으로 돌아간다.
>
> ### B-6 수정 방향 (확정)
>
> **폴백 flat 을 제거하는 것이 아니다.** RV-2가 이미 쓰고 있는 규약을 RV-1에 **역이식**한다:
>
> - **명시 표현일 때만 flat 확정** — `RV2_FLAT_RE`(`민평?\s*(?:팔자|사자)`·`플랫`·`..팔자`)
>   에 해당할 때만 `{type:'flat', value:0}`
> - **아니면 미상** — 레벨을 제시하지 않은 호가는 0bp 가 아니라 **모른다**로 표시한다
> - RV-1 UI에서 미상을 어떻게 다룰지(랭킹 제외 / 별도 표기 / 카운트 노출)는
>   **diff 보고 후 결정**한다. 지금 정하지 않는다
>
> ### 참고 — B-5용으로 확정됐던 착수 순서 (1·3단계는 B-6에 그대로 적용)
> 1. `rv-parser.js` **특성화 테스트 신설** — `rv2-parser.test.mjs`에 추가된 실데이터 이형
>    케이스를 재활용한다. 현재 동작을 **있는 그대로 고정**하는 것이 목적이다
> 2. ~~RV-2와 동일한 3줄 방어 이식~~ → B-5 강등으로 불필요
> 3. **RV-1 출력 before/after diff 보고** — (a) 커밋의 필수 산출물로 승격
>
> **RV-2 작업과 커밋·보고 단위를 분리한다. 인터리브 금지.**

| # | 항목 | 근거 |
|---|---|---|
| B-1 | **RV-1 `parseSpread` 언더N 미구현** | `rv-parser.js:148-161`이 `오버/오바`만 처리. `"언더4 팔자"`는 마지막 폴백에 걸려 **`flat 0`으로 오인**된다(민평 대비 −4bp를 0bp로 읽음). RV-1 출력이 바뀌므로 분리. 선행 조건: `rv-parser.js` 테스트 신설 |
| B-2 | **미추적 잔여물 5건 정리** | `_backup_untracked/`, `t`, `e -Force _backup_untracked...`(잘못된 PowerShell 명령이 파일명이 된 것), `taylor-module-handoff.md`, `update-curve-rv.bat` |
| B-3 | **테스트 러너 탐색 범위** | `node --test`가 `_backup_untracked/`·`tools/test-phase3.js`를 주워 실패 3건 생성. B-2 해결 시 2건 자동 소멸, `tools/test-phase3.js`는 개명 또는 `tests/`로 이전 필요 |
| B-4 | **`rv-parser.js` 멀티라인 병합의 시스템메시지 오염** | §2.2-B. RV-2는 프리패스로 우회하지만 RV-1은 여전히 노출. B-1과 함께 처리 |
| ~~B-5~~ | ~~🔴 만기 문자열이 가격 파서에 노출~~ → **강등 (실측 0건)** | §5.1 필수행 1 참조. 샘플 3,842건에서 발화 **0건**. 메커니즘이 성립하려면 만기와 방향어가 인접해야 하는데(§7.1의 합성 예시), 실데이터는 만기가 라인 앞·방향어가 뒤에 온다. **1순위 승격 근거 소멸.** 등재는 유지하되 순위 미정으로 내린다 |
| **B-6** | **🔴 `parseSpread` 폴백 flat 의 RV-1 영향 — 1순위 승격 (실측)** | `rv-parser.js:159`가 팔자/사자만 있으면 `{type:'flat', value:0}`을 반환하고, `rv-engine.js:59`가 이를 `y = 민평, basis '민평flat'`으로 채택한다. **실측: 레벨 없는 호가 2,150건 중 1,829건(85%)에 민평이 함께 있다** — 개별호가 3,596건의 **50.9%**가 "민평에 판다"는 허구의 원괴리 0으로 RV-1 횡단면에 유입된다. 버킷 중앙값(시장 베타 근사)이 0쪽으로 끌리는 규모가 예상보다 훨씬 크다. B-5 대신 **이것이 1순위**. 선행 조건은 동일(`rv-parser.js` 특성화 테스트 신설) |
| **B-7** | **`parseMinpyeong` 표기 이형 미인식 — 198건** | `민평 3.242` / `민:3.957%` / `민평3.112` / `민,3.945` 를 놓친다. 인식 범위는 `민 3.610`·`민3.517` 뿐. 이 중 **46건은 명시수익률이 함께 있어 민평만 복구되면 `explicit` 승격** → rankable 932 → 978 (+4.9%). **RV-2 계층 폴백으로 처리 가능**(언더N 과 같은 구조). §5.1.1 |
| **B-8** | **`parseActualYield` 가 `3.602팔자`(공백 없음)를 놓친다 — 43건** | `민+1원 3.602팔자` 류가 `won_unresolved` 로 떨어진다. 민평이 함께 있으므로 인식만 되면 전부 `explicit`. B-7과 같은 계층에서 함께 처리. §5.1.1 |
| B-9 | **RV-2 만기 방어의 역방향 오작동 — 9건** | `3.30 팔자` 처럼 만기 없이 수익률만 있는 전단채/PF 라인에서 `parseMaturity` 가 `3.30` 을 만기 2026-03-30(conf `low`)로 오인하고, RV-2의 `matched_text` 제거 방어가 **유일한 레벨을 지운다.** 과거 만기 호가 11건의 원인이기도 하다. 수정 방향: `confidence==='low'` 인 만기는 방어 대상에서 제외하거나, 제거 후 레벨이 사라지면 원문으로 되돌린다. **RV-2 자체 결함** — §5.1 필수행 1-a |

### 7.1 B-5 상세 — 합성 라인 실증 (실데이터 반증됨)

> **2026-08-05 정정.** 아래는 **합성 라인**에서의 실증이며, 실데이터 3,842건에서는
> **발화 0건**이다(§5.1 필수행 1). 메커니즘 자체는 맞지만 실데이터 라인 배치가
> 이를 발화시키지 않는다. 기록은 판단 근거 추적을 위해 남긴다.


`parseActualYield`·`parseSpread`는 만기 문자열을 모른 채 `(\d+\.\d+)\s*(?:팔자|사자)` 를 찾는다.
레벨이 명시되지 않은 호가에서는 **만기일의 숫자 꼬리가 수익률로 채택된다.**
`rv-engine.js:54`가 `actual_yield`를 최우선으로 쓰기 때문에 그대로 랭킹에 올라간다.

실행 확인 (`js/rv-parser.js` + `js/rv-engine.js`, 무수정 상태):

| 입력 라인 | `actual_yield` | `quoteYield` basis | 원괴리 |
|---|---|---|---|
| `도로공사975 30.5.20 팔자 (민 3.200)` | **5.2** | actual | **+200bp** |
| `중금채 27.8.3 팔자 (민 3.610)` | **8.3** | actual | **+469bp** |

→ RV-1의 **"매도호가 — 싸게 나온 순"** 리스트 최상단이 이런 허위 호가로 오염될 수 있다.
조정괴리는 버킷 중앙값을 빼지만 이 정도 크기는 상쇄되지 않는다.

**발화 조건**: 라인에 명시 수익률이 없을 때만. 명시 수익률이 있으면
(`중금채 27.8.3 3.605 팔자`) 정규식이 올바른 값(3.605)을 먼저 잡으므로 정상이다.
즉 **"레벨 없이 종목만 던지는 호가"** 가 위험 구간이다 — 빈도는 샘플로 실측한다.

**수정 방향**(별건): RV-2가 쓴 방어와 동일하게 `parseMaturity`의 `matched_text`를 가격 파서
입력에서 제거한다. `parseIssuerRaw`가 이미 만기를 인자로 받아 지우는 것과 같은 처리이므로
설계 일관성도 맞다. 선행 조건은 `rv-parser.js` 테스트 신설(B-1과 공통).
